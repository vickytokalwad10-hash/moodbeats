{
  "scripts": {
    "build:cap": "powershell -Command \"Remove-Item -Path www -Recurse -Force -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path www -Force | Out-Null; Copy-Item index.html,app.js,styles.css,manifest.json,sw.js,jiosaavnApi.js,face-api.min.js,icon.png,icon-192.png www -Force -ErrorAction SilentlyContinue; Copy-Item models www -Recurse -Force -ErrorAction SilentlyContinue\""
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "localtunnel": "^2.0.2"
  }
}
